import React from 'react'
import {PropTypes as T} from 'prop-types'

const AppearancePosters = (props) =>
  <div>
    posters
  </div>

AppearancePosters.propTypes = {
  path: T.string.isRequired
}

export {
  AppearancePosters
}
