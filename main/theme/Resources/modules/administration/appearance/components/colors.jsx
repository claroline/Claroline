import React from 'react'
import {PropTypes as T} from 'prop-types'

const AppearanceColors = (props) =>
  <div>
    colors
  </div>

AppearanceColors.propTypes = {
  path: T.string.isRequired
}

export {
  AppearanceColors
}
