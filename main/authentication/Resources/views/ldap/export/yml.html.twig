{% macro td(users, server, index, id) %}{{id}}: {% if server[id] is defined and server[id] != '' and users[index] is defined and users[index][server[id]] is defined %}{{ users[index][server[id]][0] }}{% endif %}{% endmacro %}
{% import _self as item %}
Users:
{% for i in 0..(users.count - 1) %}
    {
        {{ item.td(users, server, i, 'userName') }}
        {{ item.td(users, server, i, 'firstName') }}
        {{ item.td(users, server, i, 'lastName') }}
        {{ item.td(users, server, i, 'email') }}
        {{ item.td(users, server, i, 'userPassword') }}
        {{ item.td(users, server, i, 'code') }}
        {{ item.td(users, server, i, 'locale') }}
    }{%if not loop.last %},{% endif %}
{% endfor %}

