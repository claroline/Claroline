<div class="simple-text-widget">
    {% if text is empty %}
        <div class="empty-widget empty-placeholder placeholder-lg text-center">
            {{ 'widget_no_content'|trans({}, 'platform') }}
        </div>
    {% else %}
        {{ text|raw }}
    {% endif %}
</div>
