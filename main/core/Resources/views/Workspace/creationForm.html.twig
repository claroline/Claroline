{% extends "ClarolineCoreBundle:WorkspaceTag:layout.html.twig" %}

{% block title %}{{ parent() ~ ' - ' ~ "workspace.create" | trans({}, "platform") | striptags | raw }}{% endblock %}

{% block breadcrumb %}
    {{
        macros.breadcrumbs([
            {
                'icon': 'fa fa-book',
                'name': 'workspaces'|trans({}, 'platform'),
                'href': path('claro_workspace_list')
            },
            {
                'name': 'creation'|trans({}, 'platform'),
                'href': ''
            }
        ])
    }}
{% endblock %}

{% block section_content %}
    <div class="page-header">
        <h1>{{ 'workspace.create'|trans({}, 'platform') }}</h1>
    </div>

    <form role="form" class="form-horizontal panel panel-default" id="ws_creation_form" action="{{ path('claro_workspace_create') }}" method="post" {{ form_enctype(form) }}>
        <div class="panel-body">
            {{ form_widget(form) }}
        </div>

        <div class="panel-footer">
            <button type="submit" class="btn btn-primary">{{ 'ok'|trans({}, 'platform') }}</button>
            <a class="btn btn-default" href="{{ path('claro_workspace_list') }}" role="button">
                {{ 'cancel'|trans({}, 'platform') }}
            </a>
            <a class="btn btn-default" href="{{ path('claro_workspace_import_form') }}">{{ 'import'|trans({}, 'platform') }}</a>
        </div>
    </form>
{% endblock %}
