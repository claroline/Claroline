{% if log.details.resource is defined%}
    <strong>
        {% if log.resourceNode != null %}
            <a href="#/desktop/resources/{{ log.resourceNode.slug }}">{{ log.details.resource.path }}</a>
        {% else %}
            <em>{{ log.details.resource.path }}</em>
        {% endif %}
    </strong>
{% endif %}
