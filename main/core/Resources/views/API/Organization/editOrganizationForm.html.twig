{% extends "ClarolineCoreBundle:API:modal.html.twig" %}

{% block header %}
    {{ 'edit_organization'|trans({}, 'platform') }}
{% endblock %}
{% block form %}
    {% form_theme form 'ClarolineCoreBundle::form_theme_angular.html.twig' %}
    <form role="form"
            class="form-horizontal"
            ng-submit="eofm.submit($scope)"
            method="post" {{ form_enctype(form) }}
        >
        <div class="modal-body">
            {{ form_widget(form) }}
        </div>
        <div class="modal-footer">
            <button id="edit-organization-btn" class="btn btn-primary">{{ 'edit'|trans({}, 'platform') }}</button>
            <div class="btn btn-default" data-dismiss="modal" ng-click="$close()">{{ 'cancel'|trans({}, 'platform') }}</div>
        </div>
    </form>
{% endblock %}