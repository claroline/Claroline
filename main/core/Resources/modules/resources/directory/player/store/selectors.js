import {selectors as directorySelectors} from '#/main/core/resources/directory/store/selectors'

const EXPLORER_NAME = directorySelectors.STORE_NAME+'.directoryExplorer'

export const selectors = {
  EXPLORER_NAME
}
