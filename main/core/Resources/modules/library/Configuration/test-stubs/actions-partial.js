const config = {}

const missing = {
  actions: [
    {
      'name': 'p1',
      'type': 'administration_users'
    },
    {
      'name': 'p2',
      'type': 'administration_users'
    }
  ]
}

config.p1 = missing

export default config
