{% set resource %}
    {% include 'ClarolineCoreBundle:log:view_list_item_resource.html.twig' %}
{% endset %}

{{ ('log_' ~ log.getAction() ~ '_sentence')|trans(
        {
            '%resource%': resource,
        },
        'log'
    )|raw
}}