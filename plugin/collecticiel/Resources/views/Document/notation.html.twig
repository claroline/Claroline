{#240 : demande de JJQ de pouvoir modifier un AR dans la colonne des AR et pas seulement par "Actions" #}
<a href="#validate-modal-notation" data-toggle="modal" data-dropzone_id="{{ dropzone.id }}" 
data-document_id="{{ document.id }}" id="document_id_{{ document.id }}" name="document_id_{{ document.id }}" class="document_validate">
	<span data-toggle="tooltip" data-placement="top" data-original-title="{{ 'To post a review'|trans({},'innova_collecticiel') }}" class="fa fa-graduation-cap">
	</span>
</a>