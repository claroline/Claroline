{% extends "ClarolineCoreBundle:Resource:layout.html.twig" %}

{% block stylesheets %}
    {# Claroline CSS #}
    {{ parent() }}

    {# Resource styles #}
    <link rel="stylesheet" type="text/css" href="{{ themeAsset('claroline-distribution-plugin-announcement-announcement-resource.css') }}" />
{% endblock %}

{# Content of the Page #}
{% block content %}
    {# The react UI will be attached to this element #}
    <div
        class="page-container announcement-container"
        data-announcement="{{ _resource | api_serialize | json_encode | raw | escape }}"
        data-resource-node="{{ _resource.resourceNode | api_serialize | json_encode | raw | escape }}"
        data-workspace-roles="{{ roles | json_encode | raw | escape }}"
    >
    </div>
{% endblock %}

{% block javascripts %}
    {# Claroline JS #}
    {{ parent() }}

    {# Translations #}
    <script src="{{ url('bazinga_jstranslation_js', { 'domain': 'announcement' }) }}"></script>

    {# Resource UI #}
    <script src="{{ hotAsset('dist/claroline-distribution-plugin-announcement-announcement-resource.js') }}"></script>
{% endblock %}
