<form role="form"
      class="form-horizontal widget-content-config-form"
      action="{{ path('claro_announcements_widget_configure', {'config': config.getId()}) }}"
      method="post"
      novalidate
>
    <div class="panel-body">
        <div class="well well-sm">
            {{ 'announcements_widget_configuration_info'|trans({}, 'announcement') }}
        </div>
        {{ form_widget(form) }}
    </div>
</form>