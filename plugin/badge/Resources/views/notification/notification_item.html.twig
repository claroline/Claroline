{% extends 'ClarolineCoreBundle:notification:notification_item.html.twig' %}

{% block notificationText %}
    {% if notification.userId is not empty and notification.details.doer is defined %}
        <a href="{{ path('claro_user_profile', {'publicUrl' : notification.userId}) }}"><strong>{{ notification.details.doer.firstName ~ ' ' ~ notification.details.doer.lastName }}</strong></a>
    {% else %}
        <strong>{{ systemName }}</strong>
    {% endif %}
    {% if constant('Icap\\BadgeBundle\\Event\\Log\\LogBadgeAwardEvent::ACTION') == notification.actionKey %}
        {% include 'IcapBadgeBundle:notification:notification_badge_awarding.html.twig' %}
    {% endif %}
{% endblock %}
