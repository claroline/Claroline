const compilatioValue = 0

const toolTypes = [
  {
    type: compilatioValue,
    name: 'Compilatio',
    label: 'Compilatio',
    icon: 'fa fa-copy'
  }
]

export const constants = {
  compilatioValue,
  toolTypes
}