import React from 'react'

const ReportedComponent = () =>
  <div key='redactors' className="panel panel-default">
    reported
  </div>

ReportedComponent.propTypes = {
}

export {ReportedComponent as Reported}