<div>
  {{ 'new_certificates'|trans({}, 'platform') }}
  <ul>
      {% for el in data %}
          <li> <a href="{{ path('claro_pdf_download', {'pdf': el.pdf.getGuid()}) }}"> {{ el.user.getUsername() }}</a></li>
      {% endfor %}
  </ul>
</div>
