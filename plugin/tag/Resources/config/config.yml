plugin:
  has_options: false

  admin_tools:
    - name: claroline_tag_admin_tool
      class: tags

  resource_actions:
    - name: tags
      group: management
      decoder: administrate
      api: [GET, POST, PUT, DELETE]
