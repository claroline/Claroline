{
  "name": "claroline",
  "version": "1.0.0",
  "description": "Claroline dev dependencies and scripts",
  "scripts": {
    "fetch": "composer install && npm install && npm run bower",
    "bower": "bower update",
    "build": "node make.js",
    "webpack": "rm -rf web/dist/* && webpack"
  },
  "dependencies": {
    "claroline-core": "file:./vendor/claroline/core-bundle"
  }
}
