{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block title %}{{ parent() ~ ' - ' ~ 'personal_workspace_management' | trans({}, 'platform') | striptags | raw }}{% endblock %}

{% block breadcrumb %}
    {{
        macros.breadcrumbs([
            {
                'icon': 'fa fa-cog',
                'name': 'administration'|trans({}, 'platform'),
                'href': path('claro_admin_index')
            },
            {
                'name': 'users_management'|trans({}, 'platform'),
                'href': path('claro_admin_users_management')
            },
            {
                'name': 'personal_workspace_management'|trans({}, 'platform'),
                'href': '#'
            }
        ])
    }}
{% endblock %}

{% block section_content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ 'personal_workspace_management'|trans({}, 'platform') }}</h3>
    </div>
    <div class="list-group">
        <a class="list-group-item" href="#" id="link_group_list">
            {{ 'general'|trans({}, 'platform') }}
        </a>
        <a class="list-group-item" href="{{ path('claro_admin_workspace_tool_config_index') }}" id="link_group_list">
            {{ 'tools'|trans({}, 'platform') }}
        </a>
        <a class="list-group-item" href="{{ path('claro_admin_personal_workspace_resource_rights') }}" id="link_facet_index">
            {{ 'resources'|trans({}, 'platform') }}
        </a>

    </div>
{% endblock %}
