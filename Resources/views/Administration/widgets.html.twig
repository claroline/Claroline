{% extends "ClarolineCoreBundle:Administration:layout.html.twig" %}

{% block breadcrumb %}
    {{
        macros.breadcrumbs([
            {
                'icon': 'icon-cog',
                'name': 'administration'|trans({}, 'platform'),
                'href': path('claro_admin_index')
            },
            {
                'name': 'widget_settings'|trans({}, 'platform'),
                'href': ''
            }
        ])
    }}
{% endblock %}
{% block section_content %}
    <div class="panel-heading">
        <h3 class="panel-title"> {{ 'widget_settings'|trans({}, 'platform') }}</h3>
    </div>

    <div class="panel-body">
        <form id="create-workspace-widget">
            <select id="workspace-widgets" >
                {% for widget in wwidgets %}
                    <option value="{{ widget.getId() }}"> {{ widget.getName()|trans({}, 'widget') }} </option>
                {% endfor %}
            </select>
            <input type="submit" value="{{ 'add'|trans({}, 'platform') }}"></input>
        </form>
        <h4> {{ 'workspace'|trans({}, 'platform') }}</h4>
        <table id="widget-table" class="table table-striped table-bordered table-condensed">
            <thead>
                <tr>
                    <th>{{ 'name'|trans({}, 'platform') }}</th>
                    <th>{{ 'configuration'|trans({}, 'platform') }}</th>
                    <th>{{ 'delete'|trans({}, 'platform') }}</th>
                </tr>
            </thead>
            <tbody id="widget-workspace-table-body">
                {% for wconfig in wconfigs %}
                <tr class='row-widget-config' data-id="{{ wconfig.getId() }}">
                    <td>{% render controller('ClarolineCoreBundle:AdministrationWidget:editWidgetNameForm', {'widgetInstanceId': wconfig.getId()}) %}</td>
                    <td>
                        {% if wconfig.getWidget().isConfigurable() %}
                        <a href="{{ path('claro_admin_widget_configuration', {'widgetInstanceId': wconfig.getId()}) }}">{{ 'configure'|trans({}, 'platform') }}</a>
                        {% endif %}
                    </td>
                    <td> <a class="delete-widget" href="{{ path('claro_admin_remove_widget', {'widgetInstance': wconfig.getId()}) }}">delete</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        <hr>
        <form id="create-desktop-widget">
            <select id="desktop-widgets" >
                {% for widget in dwidgets %}
                    <option value="{{ widget.getId() }}"> {{ widget.getName()|trans({}, 'widget') }} </option>
                {% endfor %}
            </select>
            <input type="submit" value="{{ 'add'|trans({}, 'platform') }}"></input>
        </form>
        <h4> {{ 'desktop'|trans({}, 'platform') }}</h4>

        <table id="widget-table" class="table table-striped table-bordered table-condensed">
            <thead>
                <tr>
                    <th>{{ 'name'|trans({}, 'platform') }}</th>
                    <th>{{ 'configuration'|trans({}, 'platform') }}</th>
                    <th>{{ 'delete'|trans({}, 'platform') }}</th>
                </tr>
            </thead>
            <tbody id="widget-desktop-table-body">
                {% for dconfig in dconfigs %}
                <tr class='row-widget-config' data-id="{{ dconfig.getId() }}">
                    <td>{% render controller('ClarolineCoreBundle:AdministrationWidget:editWidgetNameForm', {'widgetInstanceId': dconfig.getId()}) %}</td>
                    <td>
                        {% if dconfig.getWidget().isConfigurable() %}
                        <a href="{{ path('claro_admin_widget_configuration', {'widgetInstanceId': dconfig.getId()}) }}">{{ 'configure'|trans({}, 'platform') }}</a>
                        {% endif %}
                    </td>
                    <td> <a class="delete-widget" href="{{ path('claro_admin_remove_widget', {'widgetInstance': dconfig.getId()}) }}">delete</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('bundles/clarolinecore/js/administration/widgets.js') }}" type="text/javascript"></script>
{% endblock %}
