{% extends "ClarolineCoreBundle:Workspace:layout.html.twig" %}

{% block section_content %}
<h3> {{ image.getName }} </h3>
<div><img src="{{ asset('') }}/../../files/{{ image.getHashName() }}"/></div>

<a href="{{ path('claro_resource_download') }}?ids[]={{ _resource.getResourceNode().getId() }}">{{ 'download'|trans({}, 'platform') }} </a>
{% endblock %}
