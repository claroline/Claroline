<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
            <h4>{{ 'delete' | trans({}, "platform") }}</h4>
        </div>
        <div class="modal-body">
            <p>{{ "delete_theme_confirm" | trans({}, "theme") }}</p>
            <p>{{ "irreversible_action" | trans({}, "platform") }}.</p>
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">{{ "cancel" | trans({}, "platform") }}</button>
            <button type="submit" class="btn delete btn-primary" data-dismiss="modal">{{ 'delete' | trans({}, "platform") }}</button>
        </div>
    </div>
</div>
