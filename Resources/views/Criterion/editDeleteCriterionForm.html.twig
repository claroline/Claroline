<form class="form-horizontal" method="POST" action="{{ path('innova_collecticiel_edit_remove_criterion', {'resourceId': dropzone.id, 'page': page, 'criterionId':criterion.id}) }}">
    {{ form_errors(form) }}

    <div class="container">
        {% if nbCorrection > 0 %}
            <p>
                {{ 'corrections were already created'|transchoice(nbCorrection,{},'innova_collecticiel') }}
            </p>
        {% endif %}
        {{ 'Are you sure you want to delete the %number%'|trans({'%number%': number}, 'innova_collecticiel') }}
    </div>

    <div class="container form-group form-buttons">
        <div class="btn-group pull-right">
            <a href="{{ path('innova_collecticiel_edit_criteria_paginated', {'resourceId': dropzone.id, 'page': page}) }}" class="btn btn-default">
                {{ 'Cancel'|trans({}, 'innova_collecticiel') }}
            </a>
            <button type="submit" class="btn btn-danger">
                {{ 'Confirm delete'|trans({}, 'innova_collecticiel') }}
            </button>
        </div>
    </div>
{{ form_end(form) }}
