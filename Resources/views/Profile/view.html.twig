{% extends "ClarolineCoreBundle::layout.html.twig" %}

{% block title %}{{ user.getLastName() ~ ' ' ~ user.getFirstName() | striptags | raw }}{% endblock %}

{% set canEdit = (app.user is not null and user.getId() == app.user.getId()) or is_granted('ROLE_ADMIN') %}

{% block content %}
    {{ macros.flashBox() }}
    <div class="panel panel-default panel-user">
        <div class="panel-heading relative">
            {% if canEdit %}
                <div class="dropdown float-right-top">
                    <a data-toggle="dropdown" href="#" class="btn btn-xs btn-primary">
                        <span class="icon-edit"></span> {{ 'edit' | trans({}, 'platform') }} <i class="caret"></i>
                    </a>
                    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                        <li>
                            <a href="{{ path('claro_user_profile_edit') }}">
                                {{ 'edit_profile'|trans({}, 'platform') }}
                            </a>
                        </li>
                        <li>
                            <a href="{{ path('claro_user_password_edit') }}">
                                {{ 'edit_password'|trans({}, 'platform') }}
                            </a>
                        </li>
                    </ul>
                </div>
            {% endif %}
            <h3 class="panel-title">
                {{ user.getLastName() ~ ' ' ~ user.getFirstName() }}
            </h3>
        </div>
        <div class="panel-body">
            <div class="row">
                <aside class="col-md-2 profile_aside" role="complementary">
                    <div class="avatar">
                        {% if user.getPicture() != "" %}
                            <img src="{{ asset('uploads/pictures/') ~  user.getPicture() }}" class="profile_picture">
                        {% else %}
                            <h1 class="profile_picture_placeholder"><i class="icon-user"></i></h1>
                        {% endif %}
                    </div>
                    <ul class="nav nav-pills nav-stacked contact_methods">
                        <li>
                            <a href="{{path('claro_mail_form', {'userId':user.getId()})}}">
                                <i class="icon-envelope"></i> {{ 'send_mail'|trans({}, 'platform') }}
                            </a>
                        </li>
                        <li>
                            <a href="{{path('claro_message_show', {'message': 0})}}?ids[]={{ user.getId() }}">
                                <i class="icon-comment"></i> {{ 'send_message'|trans({}, 'platform') }}
                            </a>
                        </li>
                    </ul>
                </aside>
                <div class="col-md-10">
                    <table class="table-condensed">
                        <colgroup>
                            <col class="fieldLabelCol">
                            <col class="dataCol">
                        </colgroup>
                        <tbody>
                            <tr>
                                <td>{{ 'First name' | trans({}, 'platform') }}</td>
                                <td>{{ user.getFirstName() }}</td>
                            </tr>
                            <tr>
                                <td>{{'Last name' | trans({}, 'platform') }}</td>
                                <td>{{ user.getLastName() }}</td>
                            </tr>
                            {% if user.getPhone() is not null %}
                                <tr>
                                    <td>{{ 'phone' | trans({}, 'platform') }}</td>
                                    <td>{{ user.getPhone() }}</td>
                                </tr>
                            {% endif %}
                            <tr>
                                <td colspan="2"><span class="text-muted">{{ 'registered'|trans({}, 'platform') }} {{ user.getCreationDate()| timeAgo }}</span></td>
                            </tr>
                        </tbody>
                    </table>
                    {% if '' != user.getDescription() %}<p>Description:</p> {{ user.getDescription() |raw }} </p>{% endif %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}