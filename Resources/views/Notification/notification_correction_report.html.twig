{% if notification.details.report.dropId is defined  %}
	<a href="{{ path('icap_dropzone_drops_detail', {'resourceId' : notification.details.report.dropzoneId,'dropId' : notification.details.report.dropId}) }}" target="_blank"><span class="glyphicon icon-warning-sign"></span>{{ notification.actionKey|trans({'%dropzone%' : notification.details.resource.name}, 'notification')}}</a>
{% else %}
	<a href="#"><span class="glyphicon icon-warning-sign"></span>{{ notification.actionKey|trans({'%dropzone%' : notification.details.resource.name}, 'notification')}}</a>
{% endif %}
