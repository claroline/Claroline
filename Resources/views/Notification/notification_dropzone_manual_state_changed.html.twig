	<a href="{{ path('innova_collecticiel_open', {'resourceId' : notification.details.resource.id}) }}" target="_blank">
		{{ notification.actionKey|trans({
			'%dropzone%' : notification.details.resource.name, 
			'%newState%' : notification.details.newState|trans({
			},'innova_collecticiel')
		}, 'notification') }}
	</a>
