<script type="text/javascript" src='{{ asset('bundles/frontend/jquery/jquery-1.7.1.min.js') }}'></script>
{{ tinymce_init() }}
{% form_theme form 'ClarolineCoreBundle::form_theme.html.twig' %}
{% if isDesktop == false %}
{% set isDesktop = 0 %}
{% endif %}
{% if isDefault == false %}
{% set isDefault = 0 %}
{% endif %}
<form class="form-horizontal"
      action="{{ path('claro_simple_text_update_desktop_widget_config', {'isDefault': isDefault, 'redirectToHome': app.request.isXMLHttpRequest ? 1 : 0})}}"
      method="post" {{ form_enctype(form) }}
>
    <div class="panel-body">
        {{ form_widget(form) }}
    </div>
    <div class="panel-footer">
        <button type="submit" class="btn btn-primary">{{ 'ok'|trans({}, 'platform') }}</button>
        {% if isDefault %}
            {% set cancelUrl = path('claro_admin_widgets') %}
        {% else %}
            {% set cancelUrl = path('claro_desktop_widget_properties') %}
        {% endif %}
        <a href="{{ cancelUrl }}">
            <button type="button" class="btn btn-default">{{ 'cancel'|trans({}, 'platform') }}</button>
        </a>
    </div>
</form>