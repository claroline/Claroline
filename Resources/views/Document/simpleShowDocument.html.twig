{% if document.url is not null %}
    <a target="_blank" href="{{ document.url }}">{{ 'Document n°%count%'|trans({'%count%': loop.index}, 'icap_dropzone') }} - {{ document.url }}</a>
{% else %}
    <a target="_blank" href="{{ path('icap_dropzone_open_resource', {'resourceId': dropzone.id, 'documentId': document.id}) }}">{{ 'Document n°%count%'|trans({'%count%': loop.index}, 'icap_dropzone') }} - {{ (document.type~'_type')|trans({}, 'icap_dropzone') }}</a>
{% endif %}