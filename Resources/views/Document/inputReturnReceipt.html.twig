{#
Fichier que pour l'input car la gestion de cette zone va changer.
#}
{#
Ajout "inputReturnReceipt" pour le traitement des 2 actions du bouton "Action"
#}
<input type="checkbox" class="inputReturnReceipt" id="document_id_{{document.id}}" name="documentId"
data-document_id="{{ document.id }}" value="{{ document.id }}" data-drop_id="{{ drop.id }}"
/>
