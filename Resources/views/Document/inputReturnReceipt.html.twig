{#
Fichier que pour l'input car la gestion de cette zone va changer.
#}
<input type="checkbox" id="document_id_{{document.id}}" name="documentId" data-document_id="{{ document.id }}" value="{{ document.id }}" />
