{% form_theme form "IcapDropzoneBundle::form_theme_bootstrap3.html.twig" %}

<form class="form-inline" method="POST" action="{{ path('icap_dropzone_document', {'resourceId': dropzone.id, 'documentType': documentType, 'dropId': drop.id}) }}" {{ form_enctype(form) }}>

    {% block document_header %}
        <div class="row">
            <div class="col-md-12">
                <h4>{{ documentType|trans({}, 'icap_dropzone') }}</h4>
                {#<span class="text-muted">Default template</span>#}
            </div>
        </div>
    {% endblock %}

    {% block document_body %}
        <div class="row">
            {{ form_row(form.document, {'label_col_size': ' ', 'input_col_size': 'col-md-8', 'attr': {'placeholder': (documentType~'_placeholder')|trans({}, 'icap_dropzone')}, 'label_attr': {'class': 'sr-only'},'id':'icap_dropzone_document_file_form_url' }) }}

            <div class="col-md-4">
                <div class="form-group ">
                    <button type="submit" class="btn btn-primary">
                        {{ 'Add'|trans({}, 'icap_dropzone') }}
                    </button>
                </div>
            </div>
        </div>
    {% endblock %}
    {{ form_errors(form) }}
{{ form_end(form) }}
