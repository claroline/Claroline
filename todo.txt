Option de config pour voir la liste des workspace
Option pour voir la liste des ws personnels
L'admin voit tjrs tous les workspaces
ResourceRepository->find($resourceId) => une requête pour chaque ancêtres (wtf !) // on va changer le gestionnaire de ressource.
Widget optimization
Vérifier qu'on peut pas s"inscrire 2x à un ws (registered action + liste des personnes non inscrite) + tests

=======================================

Mutiple droits dans un workspace... et finalement... on affiche lequel (si je suis admin + collaborateur, il n'en faut qu'un)
Droits => thrs le plus élevé
Tests sur le nouveau système de droits
Créer un système hybride de gestion de ressource (entre celui actuel et celui avec des instances).
Tester la création de workspace et le voter pour le workspace.

TERMINER CLARONEXT

=====================================

Gestion des ressources. Si on repasse par les instances...

Sur quoi portent les droits ? La ressource ou l'instance ?

Si c'est "un peu des 2" il faut définir les règles.

Renommer une instance != renommer le fichier ?

Ou est donc "stocké" le fichier ? Arborescence sans ws ? Dans un ws personnel ?
Comment est stocké l'arborescence ? arborescence d'instance ou de fichier ?


======================================

//Gestion des widgets trop compliquées.

=====================================

Recherche~ si on possède un grp qui a les permissions de voir... => la recherche doit prendre en compte les rôles

Ordre des messages

Rename security/rightManager en security/utilities

==========================================

PHPCS règles à rajouter

+ camelcase pour les variables
+ . SPACE ' ' ...