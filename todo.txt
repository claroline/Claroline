Option de config pour voir la liste des workspace
Option pour voir la liste des ws personnels
L'admin voit tjrs tous les workspaces
ResourceRepository->find($resourceId) => une requête pour chaque ancêtres (wtf !)
Widget optimization
//Pour le workspace
Update configureRightsAction
Update resourcesRightsFormAction
Update editResourcesRightsAction
Simplifier la config des plugins (genre rssReaderBundle: trop complexe)
~
Gestion des roles multiples lors du isGranted si l'utilisateur à plusieurs roles au sein du meme workspace
~
Vérifier qu'on peut pas s"inscrire 2x à un ws (registered action + liste des personnes non inscrite) + tests

=======================================

Entity ResourceLogger => ResourceLog
LoadEntitiesInWorkspace fixture
Mutiple droits dans un workspace... et finalement... on affiche lequel (si je suis admin + collaborateur, il n'en faut qu'un)
Tests sur le nouveau système de droits
Créer un système hybride de gestion de ressource (entre celui actuel et celui avec des instances).
Tester la création de workspace et le voter pour le workspace.
Implémentation du voter pour le workspace.
ResourceRightsRepository: avec des max et tenir compte des grp
WorkspaceRightsRepository: tenir compte des grps