
// no customization here, we just reuse the base library
// this is just to avoid having hard dependencies to the lib through the code.
import Popover from 'react-bootstrap/Popover'

export {
  Popover
}
