/**
 * Exposes buttons implemented in the `theme` module.
 */

// Implemented buttons (Component + name in registry)
import {COLOR_BUTTON, ColorButton} from '#/main/theme/buttons/color'

export {
  // button types
  COLOR_BUTTON,

  // button components
  ColorButton
}
