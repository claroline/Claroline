{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "The task id"
    },
    "name": {
      "type": "string",
      "description": "The task name"
    },
    "action": {
      "type": "string",
      "description": "The task action"
    },
    "scheduledDate": {
      "type": "string",
      "description": "The task planned date"
    }
  },
  "required": ["name", "action", "scheduledDate"],
  "claroline": {
    "ids":  ["id"]
  }
}
