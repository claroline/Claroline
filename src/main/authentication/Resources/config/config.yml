plugin:
    tools:
        - name: authentication
          icon: shield-alt

    data_sources:
        - name: my_tokens
          type: list
          context: [ desktop, administration ]
          tags: [ structure ]
