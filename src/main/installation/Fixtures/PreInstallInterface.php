<?php

namespace Claroline\InstallationBundle\Fixtures;

/**
 * PreInstall fixtures are loaded once just after the bundle migrations are executed
 * and before plugin config is processed.
 */
interface PreInstallInterface
{
}
