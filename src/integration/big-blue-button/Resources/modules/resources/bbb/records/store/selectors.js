import {selectors as baseSelectors} from '#/integration/big-blue-button/resources/bbb/store/selectors'

const LIST_NAME = baseSelectors.STORE_NAME+'.recordings'

export const selectors = {
  LIST_NAME
}
