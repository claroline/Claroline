<div class="creator">
    <div class="content row-fluid">
        <div class="border span10">
            <input
            type="text"
            maxlength="200"
            placeholder="{{ "New content page" | trans({}, 'home') }}..."
            class="content-title"
            {% if content is defined %}value="{{ content.title }}"{% endif %}
            >
        </div>

        <a class="btn btn-primary span2 create-type">{{ "Create" | trans({}, 'home') }}</a>
    </div>
    <hr>
</div>
<div class="content row-fluid">
{% for type in types %}
    {% include 'ClarolineCoreBundle:Home:type.html.twig' with {'type' : type } %}
{% endfor %}
</div>
