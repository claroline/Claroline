 {% extends 'ClarolineCoreBundle:Install:layout.html.twig' %}
{% block content %} 
<div class="span3">
    <h3> Progression</h3>
    <ol class="nav nav-list">
           {%block naviguation %}        
            {%endblock %}
        </ol>
        <p>  Progression</p>
        <div class="progress">
            <div class="bar" style="width: 60%;"></div>
        </div>
    </div>
    <div class="span6">

                  {%block data %}
        <h3> Etape 2 : Configuration de la base de donnée </h3>

        <form action="{{ path('claro_checkDbForm') }}" method="post" >
            {{ form_widget(form) }}
            <input type="submit" name="submit" value="suivant" class="btn btn-primary"/>
            </form>
            <form action="{{ path('claro_setup') }}" method="post" >
                <input type="submit" name="submit" value="retour" />
            </form>

{%endblock data %}
        </div>
        <div class="span2">
            <h3> <i class="icon-info-sign"></i> Astuces/Aides</h3>
            <div class="action">
                Si vous avez des versions supérieures à celle du tableau ,cela ne peut aller que mieux pour vous.
            </div>
        </div>
        <div class="span12">

        </div>

    </div>
    <div class="alert alert-error">
        {% for key, flash in app.session.getFlashes() %}
        {{ flash }}
        {% endfor %}
    </div>
{% endblock %}

