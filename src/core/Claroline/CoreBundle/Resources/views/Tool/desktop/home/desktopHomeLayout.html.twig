{% extends 'ClarolineCoreBundle:Desktop:layout.html.twig' %}

{% block section_content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ 'home'|trans({}, 'tools') }}</h3>
    </div>
    <div class="panel-body">
        <ul class="nav nav-tabs tabs">
            {% for adminHomeTab in adminHomeTabs %}
            <li {% if adminHomeTab.getId() == tabId %}class="active"{% endif %}>
                <a href="{{ path('claro_display_desktop_home_tabs', {'tabId': adminHomeTab.getId()}) }}" id="{{ adminHomeTab.getName() }}_link">{{ adminHomeTab.getName() }}</a>
            </li>
            {% endfor %}
            {% for userHomeTab in userHomeTabs %}
            <li {% if userHomeTab.getId() == tabId %}class="active"{% endif %}>
                <a href="{{ path('claro_display_desktop_home_tabs', {'tabId': userHomeTab.getId()}) }}" id="{{ userHomeTab.getName() }}_link">{{ userHomeTab.getName() }}</a>
            </li>
            {% endfor %}
        </ul>

        {% block homeTab %}{% endblock %}
    </div>
{% endblock %}
