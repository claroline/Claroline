[
    {% set cpt = 0 %}
    {% for key, mask in masks %}
    {
        "id": "{{key}}",
        "name": "{{mask}}"
    }
    {% set cpt = cpt+1 %}
    {% if cpt< masks|length %},{% endif %}
    {% endfor %}
]