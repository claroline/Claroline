{
    "directory": {
        "items": {
            "new": {
                "name": "new",
                "items": {
                    {% set cpt = 1 %}
                    {% for resourceType in resourceTypes %}
                    "{{ resourceType.getType() }}": {
                        "name": "{{ resourceType.getType() }}",
                        "route": "{{ path('claro_resource_creation_form', {'resourceType':resourceType.getType()}) }}",
                        "async": true
                    }
                    {% set cpt = cpt+1 %}
                    {% if cpt <= resourceTypes|length %} , {% endif %}
                    {% endfor %}
                }
            },
            "delete": {
                "name": "delete",
                "route": "{{ path('claro_resource_delete', {'instanceId': '_instanceId'}) }}",
                "async": true
            },
            "properties": {
                "name": "properties",
                "route": "{{ path('claro_resource_properties_form', {'resourceId': '_resourceId'})}}",
                "async": true
            }
        }
    },
    "file": {
        "items": {
            "download": {
                "name": "download",
                "route": "{{ path('claro_resource_custom', {'resourceType': 'file', 'action': 'download', 'instanceId': '_instanceId'}) }}",
                "async": false
            },
            "delete": {
                "name": "delete",
                "route": "{{ path('claro_resource_delete', {'instanceId': '_instanceId'}) }}",
                "async": true
            },
            "properties": {
                "name": "properties",
                "route": "{{ path('claro_resource_properties_form', {'resourceId': '_resourceId'})}}",
                "async": true
            }
       }
     },
     "text": {
        "items": {
            "properties": {
                "name": "properties",
                "route": "{{ path('claro_resource_properties_form', {'resourceId': '_resourceId'})}}",
                "async": true
            },
            "edit" : {
                "name": "edit",
                "route": " {{path('claro_resource_custom', {'resourceType': 'text', 'action': 'update', 'instanceId': '_instanceId'}) }}",
                "async": false
            }
        }
    }
}