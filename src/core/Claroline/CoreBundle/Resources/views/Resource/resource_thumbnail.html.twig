{% if currentFolder is not null %}
    <div class='current_folder' id='{{prefix}}_current_folder' data-key='{{currentFolder.id}}' data-type='{{currentFolder.getResourceType().getType()}}' data-resource-id='{{currentFolder.getResource().getId()}}'>current folder: {{currentFolder.name}}</div>
    <br>
    <br>
{% endif %}
{% for instance in resources %}
    <span id = '{{prefix}}_instance_{{instance.id}}' style='display:inline-block; width:200px'>
        <img src="{{asset('icons/' ~ instance.thumbnail)}}"><br>
        <input class='{{prefix}}_chk_instance'type='checkbox' value='{{instance.id}}'/> {{instance.name}}
        <div class='resource_bar'>
            <a href="#" id='resource_menu_{{instance.id}}' class='resource_menu' data-key='{{instance.id}}' data-type='{{instance.type}}' data-resource-id='{{instance.resource_id}}'>menu</a>
            {% if instance.type == 'directory' %}
                <a data-key='{{instance.id}}' class='link_navigate_instance' id='{{prefix}}_link_navigate_instance_{{instance.id}}' href="#">navigate</a>
            {% endif %}
        </div>
    </span>
{% endfor %}