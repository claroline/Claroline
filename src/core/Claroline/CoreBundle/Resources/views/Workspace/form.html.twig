{% extends "ClarolineCoreBundle:Workspace:layout.html.twig" %}

{% block section_content %}
    {% form_theme form 'ClarolineCoreBundle::form_theme.html.twig' %}
    <form class="form-horizontal" id="ws_creation_form" action="{{ path('claro_ws_create') }}" method="post" {{ form_enctype(form) }}>
        <fieldset>
            <legend>{{ 'workspace.create'|trans({}, 'platform') }} :</legend>
            {{ form_errors(form) }}
            {{ form_widget(form) }}
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">{{ 'ok'|trans({}, 'platform') }}</button>
                <a href="{{ path('claro_ws_list') }}">
                    <button type="button" class="btn">{{ 'cancel'|trans({}, 'platform') }}</button>
                </a>
            </div>
        </fieldset>
    </form>
{% endblock %}