{% set i = 0 %}
[
{% for group in groups %}
{% set i = i+1 %}
{
    "id": {{group.getId()}},
    "name": "{{group.getName()}}",
    "roles": "{% for role in  group.getWorkspaceRoleCollection() %}{{role.getTranslationKey()|trans({},'platform')}}{% if not loop.last %} ,{% endif %}{% endfor %}"
}
{% if i < groups|length %}, {% endif %}
{% endfor %}
]