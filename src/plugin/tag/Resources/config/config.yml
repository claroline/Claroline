plugin:
  admin_tools:
    - name: claroline_tag_admin_tool
      class: tags

  resource_actions:
    - name: tags
      group: management
      decoder: administrate
      scope: [object, collection]
