/**
 * Scorm store.
 */

import {reducer} from '#/plugin/scorm/resources/scorm/store/reducer'
import {selectors} from '#/plugin/scorm/resources/scorm/store/selectors'

export {
  reducer,
  selectors
}
