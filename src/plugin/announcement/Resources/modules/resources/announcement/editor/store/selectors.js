import {selectors as announcementSelectors} from '#/plugin/announcement/resources/announcement/store/selectors'

const FORM_NAME = `${announcementSelectors.STORE_NAME}.announcementForm`

export const selectors = {
  FORM_NAME
}
